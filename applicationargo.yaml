apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argoapp-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/hung-nashtech/SD5818_aws_infrastructure
    targetRevision: HEAD
    path: Manifest
  destination: 
    server: https://kubernetes.default.svc
    namespace: argoapp

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true